
---
interface Props {
  title: string;
}
const { title } = Astro.props;
const { subTitle } = Astro.props;
---

<div class="border-b-4  pb-4 mb-8">

    <h2 class=" text-4xl capitalize tracking-wide mb-2 font-extrabold">
        {title}
    </h2>
    <p class="">
        {subTitle}
    </p>
</div>